<odoo>
  <data>
    <record id="action_report_form_cliente" model="ir.actions.report">
      <field name="name">Formulário Reclamação Cliente</field>
      <field name="model">form.reclama.cliente</field>
      <field name="report_type">qweb-pdf</field>
      <field name="report_name">formularios.report_form_cliente_document</field>
      <field name="report_file">formularios.report_form_cliente_document</field>
      <field name="print_report_name">'Formulario_Cliente_%s' % (object.id)</field>
      <field name="binding_model_id" eval="False"/>
      <field name="binding_type">report</field>
      <field name="binding_view_types"></field>
    </record>

    <template id="report_form_cliente_document">
      <t t-call="web.html_container">
        <t t-foreach="docs" t-as="o">
          <t t-call="web.external_layout">
            <div class="page">
              <h2>Formulário de Reclamação - Cliente</h2>
              <p><strong>Utilizador:</strong> <span t-esc="o.user_id.name"/></p>
              <p><strong>Armazém:</strong> <span t-esc="dict(o._fields['armazem'].selection).get(o.armazem)"/></p>
              <p><strong>Cliente:</strong> <span t-esc="o.cliente"/></p>
              <p><strong>Código Artigo:</strong> <span t-esc="o.codigo_artigo"/></p>
              <p><strong>Descrição Artigo:</strong> <span t-esc="o.descricao_artigo"/></p>
              <p><strong>Lote:</strong> <span t-esc="o.lote"/></p>
              <p><strong>Descrição da Reclamação:</strong></p>
              <div t-esc="o.descricao_reclamacao"/>
              <p><strong>Ações de Correção:</strong></p>
              <div t-esc="o.acoes_correcao or ''"/>
            </div>
          </t>
        </t>
      </t>
    </template>
  </data>
</odoo>
