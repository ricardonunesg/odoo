<odoo>
  <data>
    <record id="view_form_reclama_cliente_list" model="ir.ui.view">
      <field name="name">form.reclama.cliente.list</field>
      <field name="model">form.reclama.cliente</field>
      <field name="arch" type="xml">
        <list>
          <field name="create_date" string="Criado em"/>
          <field name="user_id"/>
          <field name="armazem"/>
          <field name="cliente"/>
          <field name="assunto"/>
        </list>
      </field>
    </record>

    <record id="view_form_reclama_cliente_form" model="ir.ui.view">
      <field name="name">form.reclama.cliente.form</field>
      <field name="model">form.reclama.cliente</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <div class="oe_title">
              <h1><field name="assunto" placeholder="Assunto"/></h1>
            </div>

            <group>
              <field name="user_id"/>
              <field name="armazem"/>
              <field name="armazem_outro" attrs="{'invisible': [('armazem','!=','outra')]}"/>
            </group>

            <group>
              <field name="cliente"/>
              <field name="codigo_artigo"/>
              <field name="descricao_artigo"/>
              <field name="lote"/>
            </group>

            <group>
              <field name="descricao_reclamacao"/>
              <field name="acoes_correcao"/>
            </group>

            <group string="Documento da Reclamação">
              <label string="(Email do cliente, whatsapp do cliente, documentos enviados pelo cliente, fotos, etc)"/>
              <field name="anexos_ids" widget="many2many_binary"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>
  </data>
</odoo>
